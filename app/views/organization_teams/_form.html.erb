<%= form_for(organization_team) do |f| %>
  <% if organization_team.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(organization_team.errors.count, "error") %> prohibited this organization_team from being saved:</h2>

      <ul>
      <% organization_team.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :organization_id %>
    <%= f.text_field :organization_id %>
  </div>

  <div class="field">
    <%= f.label :team_id %>
    <%= f.text_field :team_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
