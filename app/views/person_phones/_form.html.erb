<%= form_for(person_phone) do |f| %>
  <% if person_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(person_phone.errors.count, "error") %> prohibited this person_phone from being saved:</h2>

      <ul>
      <% person_phone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %>
    <%= f.text_field :person_id %>
  </div>

  <div class="field">
    <%= f.label :phone_id %>
    <%= f.text_field :phone_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
