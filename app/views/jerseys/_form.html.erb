<%= form_for(jersey) do |f| %>
  <% if jersey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jersey.errors.count, "error") %> prohibited this jersey from being saved:</h2>

      <ul>
      <% jersey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %>
    <%= f.text_field :person_id %>
  </div>

  <div class="field">
    <%= f.label :team_id %>
    <%= f.text_field :team_id %>
  </div>

  <div class="field">
    <%= f.label :number %>
    <%= f.number_field :number %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
