<%= form_for(team_person) do |f| %>
  <% if team_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(team_person.errors.count, "error") %> prohibited this team_person from being saved:</h2>

      <ul>
      <% team_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id_id %>
    <%= f.text_field :person_id_id %>
  </div>

  <div class="field">
    <%= f.label :team_id_id %>
    <%= f.text_field :team_id_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
